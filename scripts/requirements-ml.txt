# ML dependencies — optional, install when needed (cover/training features)
# These are large packages, install separately with:
#   pip install -r requirements-ml.txt
soundfile>=0.12.0
numpy>=1.26.0
torch>=2.2.0
torchaudio>=2.2.0
demucs>=4.0.0
transformers>=4.36.0
praat-parselmouth>=0.4.3
faiss-cpu>=1.8.0
ffmpeg-python>=0.2.0
