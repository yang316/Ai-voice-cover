{
  "name": "ai-voice-cover",
  "version": "0.2.4",
  "private": true,
  "description": "AI Voice Cover - 人声分离 + 声音转换 + 混合输出",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd .venv && source bin/activate && cd .. && uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload",
    "dev:frontend": "cd frontend-vue && npm run dev",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "build:sidecar": "pyinstaller --onefile --name ai-voice-cover-server --add-data backend:backend --add-data frontend:frontend --hidden-import sqlite3 --collect-submodules sqlite3 scripts/sidecar-launcher.py"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "concurrently": "^9.0.0"
  }
}
